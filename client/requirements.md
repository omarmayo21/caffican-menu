## Packages
qrcode.react | Generate and display QR codes for the digital menu tables
framer-motion | Page transitions and smooth scroll-triggered animations

## Notes
- "CaffiCan" is the exact brand name.
- Custom fonts (Playfair Display, Poppins) are handled via CSS `@import` and variables in `index.css`.
- The admin dashboard uses a dark theme wrapper to fulfill the luxury dark mode requirement.
